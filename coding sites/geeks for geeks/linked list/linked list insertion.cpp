void linked_list_insertion(node* head){
	node* new_list_head = NULL;
	node* new_list_tail = NULL;
	node* temp = head;
	int value;
	node* new_list;
	while(temp!=NULL){
		value = temp->data;
		new_list = new node(value)
		if(temp->next->data == 0){
			
		}
		temp = temp->next->next;
	}


}